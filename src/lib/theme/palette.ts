/**
 * Brand palette constants — mirror CSS vars in brand-palette.css for JS/Canvas use.
 */
export const brandPalette = {
  /** OKLCH hue (0–360). Change in brand-palette.css :root --brand-hue */
  hue: 264,
  /** 3D hero wireframe — sync with theme accent */
  heroMeshColor: "#8b7cf6",
} as const;
